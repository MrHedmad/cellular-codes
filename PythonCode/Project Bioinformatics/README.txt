The _pychace_ is required for some python inner working when loading package files such as "chone.py".
"chone.py" is a version of Chapter One.py for easy import into Chapter two and so on.

Do *not* delete chone.py, chtwo.py, etc... or the _pychahe_ folder!
