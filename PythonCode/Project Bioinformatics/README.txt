The _pychace_ is required for some python inner working when loading function-package files such as "chone.py".
"chone.py" is a version of Chapter_One.py for easy import into Chapter two and so on.

Do *not* delete chone.py, chtwo.py, etc... or the _pycache_ folder!
